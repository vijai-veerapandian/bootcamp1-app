services:
  backend:
    build: ./backend
    environment:
      CORS_ORIGINS: "http://localhost:8080"
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "8080:8080"
